import 'package:flutter/material.dart';

Widget RatingStar() {
  return Row(
    children: const [
      Icon(
        Icons.star,
        color: Colors.amber,
        size: 20,
      ),
      Icon(
        Icons.star,
        color: Colors.amber,
        size: 20,
      ),
      Icon(
        Icons.star,
        color: Colors.amber,
        size: 20,
      ),
      Icon(
        Icons.star,
        color: Colors.amber,
        size: 20,
      ),
      Icon(
        Icons.star,
        color: Colors.amber,
        size: 20,
      ),
    ],
  );
}
